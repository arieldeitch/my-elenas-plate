# Run — points v1 and input UX

Implemented an independent, transparent internal daily-points budget (default 30 per profile), immutable v1 entry snapshots with historical fallback, selected-day totals, live quantity preview, and narrow offline/realtime profile-budget synchronization. Typed search results now always open quantity selection; coffee retains its editor and trusted favorite/recent chips retain quick add with an edit action. Keyboard-safe sheets now anchor synchronously to VisualViewport and use only obscured, nearest, instant correction.

The additive migration and reviewed apply/read-only verify scripts were created but not applied to production. No calories, macros, proprietary formulas, auth changes, service-role changes, or historical migration edits were made.
