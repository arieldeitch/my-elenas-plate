# Task prompt — run of 2026-09-18 (third run, M2) — verbatim as received; list markers may be normalized by prettier

Continue the Nutrition App project directly from the current repository state.

Do NOT restart takeover/discovery.

Current expected starting point:

`main @ ff25b29`

Read the latest run record and canonical repo documentation first, then verify actual HEAD/origin state before modifying anything.

The previous run established:

- M1 implementation is merged and tested.
- Production is still blocked only by owner-account actions in Supabase + Lovable.
- Current live deployment is still the old pre-M1 demo deployment.
- Runtime fail-safe now prevents a future production/shared build from silently degrading into localStorage-only demo behavior.
- `/build-info.json` exists.
- executable release preflight exists.
- the real Lovable runtime-config root cause is understood.
- M2 step 1, PartnerGlance, is implemented.
- current main is green and pushed.

Do not re-investigate these facts unless current evidence contradicts them.

# OPERATING MODE

Work autonomously.

If you encounter an external account blocker already documented, record that it is still blocked and MOVE ON.

Do not spend this run repeatedly attempting inaccessible Supabase/Lovable operations.

Do not ask Ariel for routine engineering decisions.

GitHub remains implementation truth.

Preserve the current M1 architecture unless evidence proves it defective.

---

# PRIMARY OBJECTIVE

Advance the app from:

"technically recovered nutrition logger"

toward:

"a frictionless daily couple nutrition product Ariel and Elena actually enjoy using."

The next product milestone is:

# M2 — DAILY COUPLE EXPERIENCE

Focus this run primarily on the mobile home screen and the logging loop.

The experience should answer within a few seconds:

1. How am I doing today?
2. How is my partner doing today?
3. What did each of us eat recently?
4. What is the fastest possible way to log the next thing?
5. Can I trust that what I see is actually shared/cloud-backed?

Do not create a feature explosion.

Build a coherent vertical slice.

---

# PHASE 1 — FAST CURRENT-STATE CHECK

Quickly verify:

- current branch;
- HEAD;
- origin/main;
- clean/dirty tree;
- previous run docs;
- current M2 implementation;
- existing mobile home screen.

Do not repeat architecture discovery.

Then inspect the running/local UI at a phone-sized viewport before designing changes.

---

# PHASE 2 — COMPLETE THE PARTNER GLANCE EXPERIENCE

The existing PartnerGlance was M2 step 1.

Now make it genuinely useful.

Review its current implementation visually and functionally.

It should provide an immediate, quiet snapshot of the partner's current day.

At minimum evaluate displaying:

- partner name;
- consumed calories;
- calorie target/progress;
- protein consumed / target;
- latest meaningful food/meal activity;
- current fasting state if the data already exists reliably;
- workout/activity indicator only if the existing data model supports it cleanly;
- freshness/sync indication where useful.

Do not overload the card.

Prioritize:

**calories + protein + recent activity**

over secondary information.

If fasting/workout information is incomplete or architecturally awkward, do not manufacture it.

---

# PHASE 3 — REDESIGN THE HOME INFORMATION HIERARCHY

Inspect the complete home screen.

Reduce noise.

The home screen should have a clear visual hierarchy roughly centered on:

## ME

My today status.

## PARTNER

Elena/Ariel today status.

## ACTION

Fastest path to log food.

The exact design is yours to determine from the existing application.

Do not blindly create three giant cards.

Look at the current components and produce the cleanest mobile layout that fits the product.

Important:

The app is primarily used on phones.

Test explicitly around a Pixel-7-class viewport and at least one narrower mobile viewport.

Avoid needing horizontal scrolling.

Avoid long vertical dashboards full of administrative metrics.

---

# PHASE 4 — LOWER-FRICTION FOOD LOGGING

Analyze how many interactions are currently required to log a normal food/meal.

Then reduce friction materially.

Target the common case.

Potential improvements to investigate:

- strong persistent or highly visible quick-add entry;
- recent foods;
- frequently used foods;
- repeat a recent meal;
- sensible default ownership = current person;
- sensible default date/time = now;
- fewer modal transitions;
- preserve advanced editing without forcing it into the common path.

Do NOT implement all ideas automatically.

Choose the smallest set that makes logging noticeably faster.

Measure the before/after flow in interactions/taps and report it.

Do not sacrifice correctness or food ownership for speed.

---

# PHASE 5 — MAKE OWNERSHIP OBVIOUS

One of the core product risks is confusion between Ariel data and Elena data.

Audit the relevant M2 screens for ownership clarity.

A user should never wonder:

"Am I adding/editing this for me or for my partner?"

Use subtle but unmistakable ownership indicators.

Avoid making the interface look like an admin multi-user system.

Identity should feel personal, not technical.

Ensure:

- adding;
- editing;
- viewing recent entries;
- partner glance;
- historical day navigation

all preserve correct person attribution.

Add tests where useful.

---

# PHASE 6 — TRUST / SYNC FEEDBACK

The entire product proposition depends on shared truth.

Now that production has explicit shared/demo modes, make the user-facing cloud state appropriately understandable.

Do NOT expose technical Supabase terminology.

Do NOT add constant noisy status banners.

Instead determine whether the UI needs a subtle trust signal such as:

- synced;
- updating;
- offline;
- could not sync.

If existing infrastructure supports it, implement a compact and calm state indicator.

Most of the time it should disappear into the interface.

Errors must be noticeable.

Never falsely report successful synchronization.

---

# PHASE 7 — M1 RELEASE READINESS

Do not attempt inaccessible owner actions repeatedly.

But make the post-owner-action validation path as close to one command/checklist as reasonably possible.

Review:

- `npm run preflight -- --live`
- build-info behavior;
- migration verification scripts;
- M1 live acceptance instructions.

If there is duplication or ambiguity, simplify it.

Goal:

once Ariel completes the 3 external actions, Claude should be able to resume and determine in minutes whether M1 is genuinely live.

If useful, add a single documented "M1 release acceptance" entrypoint.

Do not automate destructive production writes.

---

# PHASE 8 — OLD LOCAL PHONE DATA

Do NOT build an automatic importer during this run.

Current fact:

pre-cloud entries on Ariel's and Elena's phones may exist only in localStorage.

The previous run asked whether they should be discarded or migrated.

Until Ariel explicitly requests migration:

- preserve them;
- do not automatically import them;
- do not delete them;
- do not mix them into production shared truth.

If useful, inspect the current localStorage schema and document whether a safe future one-time import is technically feasible.

Keep this investigation short.

Do not let it consume the M2 run.

---

# PRODUCT DESIGN PRINCIPLES

This is a private couple nutrition application.

It is NOT:

- a medical dashboard;
- a CRM;
- an admin console;
- an analytics product.

The emotional feel should be:

- calm;
- lightweight;
- supportive;
- shared;
- quick;
- trustworthy.

Every additional element on the home screen has to earn its space.

Prefer direct information over decorative UI.

Prefer one-tap/two-tap actions over menus.

Prefer progressive disclosure for detail.

Avoid excessive badges, panels and metrics.

---

# VISUAL QUALITY

Do actual visual inspection.

Do not accept "tests pass" as UI validation.

Check:

- hierarchy;
- spacing;
- typography;
- density;
- touch targets;
- empty states;
- loading states;
- partner card states;
- long food names;
- Hebrew/RTL behavior if already supported;
- narrow phone screens.

If screenshots or Playwright snapshots are available, use them to inspect changes.

Do not make a wholesale visual redesign unrelated to this milestone.

---

# TESTING REQUIREMENTS

For completed work run appropriate:

- TypeScript;
- lint;
- unit tests;
- integration tests;
- hermetic/browser tests;
- production build;
- release preflight where relevant.

Add focused tests for new behavior.

Pay particular attention to:

- person ownership;
- partner hydration;
- changing viewed dates;
- fast logging;
- persistence;
- production/runtime guard.

No flaky sleep-based tests if deterministic synchronization can be used.

---

# GIT / DELIVERY

Work in coherent commits.

Before committing:

- inspect diff;
- remove debug code;
- verify no secret is present;
- ensure generated artifacts belong in Git before adding them.

Push completed green work.

Routine merges are authorized where appropriate.

No force-push.

---

# DOCUMENTATION

Continue the existing repo-native project record.

Update only useful canonical documents.

Record:

- chosen M2 scope;
- UX decisions;
- implementation;
- tests;
- remaining M1 external blockers;
- next milestone.

Save this run's prompt and final report according to the existing convention.

Update the canonical project last-update timestamp.

Do not create redundant status documents.

---

# DO NOT DO

Do not:

- rebuild the application;
- redesign the database unnecessarily;
- introduce a new state-management architecture without need;
- introduce realtime merely because it is a couple app;
- create new Supabase tables for UI convenience;
- expose service_role;
- commit private secrets;
- silently migrate phone-local data;
- claim live M1 success without production evidence;
- spend the run documenting known account-access failures.

---

# DEFINITION OF SUCCESS

A strong result from this run should include:

1. PartnerGlance becomes genuinely useful.
2. Home screen information hierarchy is noticeably cleaner.
3. Normal food logging requires materially less friction.
4. Person ownership remains unmistakable.
5. Shared/cloud state is trustworthy and understandable.
6. Mobile experience is visually verified.
7. M1 remains ready for immediate production closure once the owner actions are completed.
8. Tests/build remain green.
9. Changes are committed and pushed.
10. The next product milestone is already selected.

---

# FINAL REPORT

Use exactly these sections:

## STATUS

Overall:
`GREEN`, `YELLOW`, or `RED`

Also state separately:

- M1
- M2
- repository

## STARTING STATE

Branch, SHA, worktree, origin state.

## PRODUCT BEFORE

Briefly describe the actual home/logging UX you found at the start of the run.

## M2 IMPLEMENTED

Exactly what changed.

## PARTNER EXPERIENCE

What Ariel can now understand about Elena and vice versa.

## LOGGING FRICTION

Describe the normal logging flow before vs after.
Include approximate tap/interaction count where meaningful.

## OWNERSHIP

How person attribution is made clear and what was tested.

## SYNC / TRUST

What the user now sees and what it actually guarantees.

## MOBILE UX

What viewports were inspected and important visual findings.

## M1 READINESS

Current blockers and whether any release/acceptance tooling changed.

## LOCAL PHONE DATA

Only relevant findings.
No migration unless explicitly authorized.

## TESTS

Exact checks and results.

## GIT

Ending SHA(s), branch and push state.

## DOCUMENTATION

Exact files updated.

## YOU

Only things Ariel must genuinely do himself.

Do not repeat engineering tasks Claude can perform.

## NEXT

Choose the next concrete milestone.

## RUN TIMESTAMP

Exact local date and time at run completion.

Work autonomously and use the available session productively.
Do not stop early merely because M1 production access is still owner-blocked.
