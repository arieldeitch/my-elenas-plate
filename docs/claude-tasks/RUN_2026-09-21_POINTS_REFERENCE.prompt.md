# Task prompt — run of 2026-09-21 (thirteenth run, canonical points reference from `ניקוד.xlsx`) — verbatim as received; list markers may be normalized by prettier

אנחנו מוסיפים כעת לאפליקציית התזונה הזוגית את מאגר הניקוד המצורף בקובץ `ניקוד.xlsx`.

בצע את העבודה מקצה לקצה. אל תסתפק בניתוח או בהמלצות.

## פתיחת הריצה

1. בצע את בדיקת הפתיחה המחייבת מול מערכת ההפעלה והנחיות הפרויקט.
2. קרא את מסמכי העבודה הקיימים בריפו, לרבות `CLAUDE.md`, `AGENTS.md`, תיעוד המוצר וה־migrations, ככל שהם קיימים.
3. אמת את מצב `main`, את הקשר ל־GitHub ואת פרויקט Supabase הנכון: `rqgoiuztphkcvbwtbxbj`
4. אל תפעיל Docker או סביבת פיתוח מקומית כבדה אלא אם הדבר הכרחי לבדיקה מסוימת ואין חלופה קלה יותר. אם הפעלת Docker, הסבר למה וכבה אותו בסיום.
5. לפני שינוי כלשהו, מפה את מודל המאכלים, הארוחות, הניקוד והמשתמשים הקיים. אל תבנה מערכת מקבילה ללא צורך.

## מטרת המימוש

להפוך את `ניקוד.xlsx` למקור ייחוס קנוני עבור ניקוד מאכלים באפליקציה, כך ש:

- מאכלים קיימים יקבלו התאמה נכונה למאגר בלי לפגוע בהיסטוריה.
- בעת בחירת מאכל יוצגו הכמות הקנונית, היחידה, הניקוד וקטגוריית המזון.
- הזנת כמות שונה תחשב ניקוד יחסי רק כאשר קיימת המרה חד־משמעית ובטוחה.
- כל מאכל חדש יטופל בהתאם ללוגיקה ולדפוסים שבמאגר.
- המערכת לא תמציא ניקוד כאשר אין התאמה מספקת.

## עובדות שאומתו בקובץ

הקובץ מכיל בקירוב: 1,394 רשומות ניקוד תקינות; 1,242 שמות מאכלים ייחודיים; 128 שמות שמופיעים ביותר מרשומה אחת; ניקוד בחצאי נקודות, לצד ערכים שלמים ו־0; קטגוריות (חלבון מהחי, חלבון מהצומח, דגנים ופחמימות, קטניות, ירקות, פירות, מוצרי חלב, שומנים, משקאות, אוכל בחוץ, חטיפים מלוחים, חטיפים ממרחים ומתוקים, רטבים אבקות חלב ותבלינים, סלטים מוכנים, תוספת חלבון ב־0 נקודות). יש גם שגיאות מקור, טקסטים קטועים, כותרות חוזרות וקטגוריה אחת שנכתבה באופן קטוע. אין לייבא אותן בצורה עיוורת.

## כללי ניקוי וייבוא

בנה תהליך import/reconciliation שניתן להריץ שוב באופן בטוח ו־idempotent. השתמש בארבע העמודות הראשונות בלבד; התעלם מהטבלה הצדדית `מה אכלתי`, משורות ריקות, מכותרות חוזרות, משורות שאינן רשומות מזון, מכפילויות זהות לחלוטין. שמור את ערכי המקור הגולמיים (`source_name`, `source_quantity_text`, `source_points`, `source_category`, `source_row`, `source_version`). אל "תתקן" אוטומטית שם קטוע — `needs_review`. נרמל לצורכי חיפוש בלבד; שמור תמיד את שם התצוגה המקורי; אל תאחד מאכלים על בסיס fuzzy match. תקן `חלבון מהח` → `חלבון מהחי` רק אם זו השגיאה המדויקת, ותעד. סתירות (`יוגורט טבעי 2.9% שומן -200ג, 2` 4/5; `משקה אורז 1% שומן` 2.5/3) — `conflict/needs_review`, לא ברירת מחדל, לא דורסות נתוני משתמש.

## מודל הנתונים

התאם לארכיטקטורה הקיימת (`food_definitions` / `food_portions` / `food_aliases` / provenance / סטטוס / `created_by`). הפרד בין מאגר קנוני משותף, מאכל מותאם של אריאל/אלנה, ומופע בארוחה היסטורית (snapshot של שם, כמות, יחידה וניקוד).

## מנוע הניקוד

התאמה מדויקת → כינוי מאומת → המרה יחסית באותה יחידת בסיס (גרם/מ״ל/יחידות; כף/כפית רק מול מנת ייחוס מאותו סוג) → אין המרה אוטומטית כוס→גרם, יחידה→גרם, מנה→גרם, לפני/אחרי בישול, רגיל→לייט, כללי→מותג, מתכון→רכיב. חצאי נקודות ללא עיגול לשלם. כמה התאמות → המשתמש בוחר. אין התאמה → מאכל חדש עם שם/כמות/יחידה/קטגוריה; הצעת ניקוד רק כ־`הצעה לבדיקה` עם מקור ורמת ודאות; אישור המשתמש; ללא ממוצע קטגוריה או ניחוש AI. לאחר אישור — שמירה לשימוש חוזר לפי מודל ההרשאות.

## חריגי 0 נקודות

הפרדה בין `base_points` ל־`applied_points`; מנגנון זכאות לפי משתמש/יום/ספירת ניצול; אם הכלל אינו ניתן להוכחה — ייבוא כ־conditional בלי החלה אוטומטית; אין הטבה כפולה על אותה מנה.

## חיפוש וחוויית שימוש

חיפוש מהיר בעברית, תוצאות קומפקטיות (שם / מנת ייחוס · נקודות · קטגוריה), בחירת וריאציה, תיעדוף מאכלים אחרונים בלי להסתיר את המקור הקנוני, שינוי כמות לפני הוספה, תצוגת ניקוד לפני אישור; `needs_review`/`conflict` לא כתוצאה רגילה. ללא redesign רוחבי.

## שמירה על נתונים והרשאות

אין מחיקה, אין דריסת היסטוריה, reconciliation בטוח אוטומטי / עמום לדוח review, הפרדה אריאל/אלנה ו־RLS, migrations backward-compatible או עם rollback.

## בדיקות קבלה מחייבות

16 בדיקות: התאמה מדויקת; יחסי בגרמים; יחסי במ״ל; חצאי נקודה; חסימת לפני/אחרי בישול; חסימת כוס→גרם; וריאציות; התנגשות; מאכל חדש; שמירה ושימוש חוזר; snapshot היסטורי; הפרדה והרשאות; 0 מותנה ללא זכאות; מניעת הטבה כפולה; importer חוזר ללא כפילויות; UI במסך צר. וכן end-to-end בסביבת בדיקה בטוחה.

## תוצרי הריצה

קוד ומיגרציות; dataset נקי עם versioning ו־provenance; דוח import; דוח reconciliation; תיעוד קצר; בדיקות ותוצאותיהן; commit ו־push לענף עבודה; ללא merge ל־main או migration בייצור בלי שערי האישור; עצירה בשער הנכון עם מה שחסר.

## דיווח סיום

STATUS / SYSTEM CHECK / WHAT CHANGED / SCORING LOGIC / IMPORT RESULTS / CONFLICTS / NEEDS REVIEW / DATA SAFETY / TESTS / DEPLOYMENT / CONTROL TOWER / CHIEF OF STAFF REPORTING / YOU / NEXT. אל תטען שהייבוא, הפרסום או הדיווח בוצעו בלי ראיה טכנית. חותמת תאריך ושעה לפי אזור הזמן של ישראל.

---

Follow-up answer (after the file was not found in the repo / disk / Drive / Gmail): "The source file is now available at: docs/data/nutrition-points-source.xlsx. Continue autonomously with the full implementation described in the previous prompt. Treat this Excel file as source input, preserve provenance, perform the required validation and conflict handling, and do not stop after analysis." (The file arrived as `docs/nutrition-points-source.xlsx.xlsx` and was moved to the stated path; md5 identical to the two Downloads copies.)
