# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
.wrangler
*.local

# Repo uses bun (bun.lock). Ignore the npm lockfile from local installs.
package-lock.json

# Test coverage output
coverage

# Environment files. Local `.env*` files are private (they may point anywhere).
# Tracked on purpose: `.env.example` (template) and `.env.production` (PUBLIC
# values only — project URL, runtime target, anon/publishable key; DEC-025).
.env
.env.*
!.env.example
!.env.production

# Playwright E2E artifacts
test-results
playwright-report
/e2e/.cache

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
